[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gradebook-core"
version = "0.1.0"
description = "Core logic for GradeBook: models, validation, statistics"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/mspro72/gradebook"